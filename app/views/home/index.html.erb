<!DOCTYPE html>
<html>
  <head>
    <title>RadiantRails</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" %>
    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
        <nav class="nav-container">
            <div class="logo-box">
                LOGO
            </div>
            <div class="parent-links">
                <a class="social"><%= link_to "About", new_session_path, class: 'social-links' %></a>
                <a class="social"><%= link_to "Contact", new_session_path, class: 'social-links' %></a>
                <% if Current.user %>
                    <a class="social"><%= link_to "Gestionar secciones", secciones_path, class: 'social-links' %></a>
                <% end %>
            </div>
            <div class="icon">
                <a><%= link_to "Login", login_path, class: 'sm-icon' %></a>
            </div>
        </nav>
    </header>
  </body>
</html>

<style>
    *{
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        color: #ECE9DD;
    }
    .container{
        display: flex;
        justify-content: space-between;
    }
    .parent-links{
        font-size: 1rem;
    }
    .parent-links, .icon{
        display: flex;
        align-items: center;
        justify-content: center;
    }
    .logo{
        height: 42px;
    }
    .logo:hover{
        opacity: 0.8;
    }
    .social{
        display: flex;
        justify-content: center;
        align-items: center;
    }
    .social-links{
        text-decoration: none;
        padding: 0 2.5rem;
        margin: 0;
        color: #ECE9DD;
        font-weight: 500;
    }
    .social-links:hover{
        opacity: 0.8;
    }
    .sm-icon{
        padding: 0 1rem;
        margin: 0;
        color: #ECE9DD;
    }
    .sm-icon:hover{
        opacity: 0.8;
    }
    nav{
        font-size: 20px;
        font-weight: 700;
        display: flex;
        justify-content: space-between;
        padding-top: 32px;
    }

    /* body{
        height: 100vh;
        padding: 5px 7% 5px;
    } */
</style>
